package com.example.ems.support.entity;

public enum SupportNotificationType {
    NEW_TICKET,
    NEW_REPLY,
    STATUS_CHANGED,
    PRIORITY_CHANGED,
    ASSIGNED,
    REASSIGNED,
    CLOSED,
    REOPENED,
    SLA_WARNING,
    SLA_BREACHED,
    MERGED
}
